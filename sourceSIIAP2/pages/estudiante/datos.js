import React from "react";
import { Text, View } from "react-native";

const Datos = ({ navigation }) => {
    return (
        <View>
            <Text>Datos</Text>
        </View>
    );
}

export default Datos;