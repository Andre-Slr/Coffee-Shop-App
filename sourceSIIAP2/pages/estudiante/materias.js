import React from "react";
import { Text, View } from "react-native";

const Materias = ({ navigation }) => {
    return (
        <View>
            <Text>Materias</Text>
        </View>
    );
}

export default Materias;